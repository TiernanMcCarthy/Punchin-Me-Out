using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KillPlayer : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}

// Circle radius
// Pythagoras calculation between the centre of the circle and the player
// Check that the calculation is smaller than radius of the circle
